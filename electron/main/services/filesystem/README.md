# FileSystem Service

Native file system access via Node.js `fs` module.

## Responsibilities

- Read file contents and directory trees from disk paths
- Open native file/folder picker dialogs (`dialog.showOpenDialog`)
- Write files (PDF export, reports)
- Recursive directory scanning with progress events
- Scan cancellation via abort tokens
